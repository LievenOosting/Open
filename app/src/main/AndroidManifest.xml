<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.qiuchunjia.sample">

    <application
        android:name="com.qcj.common.base.AppContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Transparent">
        <activity
            android:name=".ListTestActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListHadHeadActivity" />
        <activity android:name=".ViewPagerTestActivity" />
        <activity android:name=".ViewPager1TestActivity" />
        <activity android:name=".ViewPager2TestActivity" />
        <activity android:name=".BezierActivity" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>